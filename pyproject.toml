[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "quick-tkinter"
version = "0.0.1"

[tool.setuptools]
py-modules = ["quick_tkinter", "abc_whole_mro", "psg_debugger"]